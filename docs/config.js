/**
 * Optional: set to your deployed BudgetPlay API (no trailing slash) for full Claude + shared DB.
 * Example: window.BUDGETPLAY_API_BASE = 'https://budgetplay-api.onrender.com';
 *
 * Leave empty for static hosting (GitHub Pages): uses data/*.json + localStorage for votes.
 */
window.BUDGETPLAY_API_BASE = window.BUDGETPLAY_API_BASE || '';
